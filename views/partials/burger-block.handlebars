<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-iseaten="{{eaten}}">
		{{#if eaten}}CONSUME{{else}}CONSUME MORE{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">UNDO BURGER</button>
</li>
